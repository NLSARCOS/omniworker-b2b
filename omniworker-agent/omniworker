#!/usr/bin/env python3
"""
OmniWorker Agent CLI launcher.

This wrapper should behave like the installed `omniworker` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from omniworker_cli.main import main
    main()
