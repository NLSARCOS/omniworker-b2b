name: openai
version: 1.0.0
description: "OpenAI image generation backend (gpt-image-2). Saves generated images to $OMNIWORKER_HOME/cache/images/."
author: OmniWorker
kind: backend
requires_env:
  - OPENAI_API_KEY
