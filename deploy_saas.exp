#!/usr/bin/expect -f
set timeout 600
spawn ssh -o StrictHostKeyChecking=no -p 2424 root@217.76.62.37 "cd /opt/omniworker && git pull && cd omniworker-saas && docker compose up -d --build"
expect "*assword:*"
send "19748961Nl\r"
expect eof
